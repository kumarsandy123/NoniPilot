<UserControl x:Class="NoniPilot.Desktop.Controls.StatusPill"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource PanelElevated}" BorderBrush="{StaticResource PanelBorder}"
            BorderThickness="1" CornerRadius="10" Padding="8,4">
        <StackPanel Orientation="Horizontal">
            <Ellipse x:Name="Dot" Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center" />
            <TextBlock x:Name="TextElement" Foreground="{StaticResource TextDim}" FontSize="11" VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
