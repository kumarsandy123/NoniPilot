<UserControl x:Class="NoniPilot.Desktop.Pages.ApplicationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24,20">
        <StackPanel>
            <TextBlock Text="Applications" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16" />

            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" x:Name="LaunchBox" Background="#0E1420" Foreground="{StaticResource TextPrimary}"
                             BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="10,6" VerticalContentAlignment="Center"
                             ToolTip="App name or full path, e.g. notepad or chrome" KeyDown="LaunchBox_KeyDown" />
                    <Button Grid.Column="1" Content="Launch" Style="{StaticResource PillButton}" Margin="8,0,0,0"
                            Background="{StaticResource AccentGradient}" Foreground="White" Click="Launch_Click" />
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}" Padding="0" MinHeight="360">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="16,12">
                        <Button DockPanel.Dock="Right" Content="Refresh" Style="{StaticResource PillButton}" Click="Refresh_Click" />
                        <TextBlock Text="Running applications" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center" />
                    </DockPanel>
                    <ListView Grid.Row="1" x:Name="ProcessList" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimary}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Window title" Width="360" DisplayMemberBinding="{Binding MainWindowTitle}" />
                                <GridViewColumn Header="Process" Width="160" DisplayMemberBinding="{Binding ProcessName}" />
                                <GridViewColumn Header="PID" Width="80" DisplayMemberBinding="{Binding ProcessId}" />
                                <GridViewColumn Header="" Width="180">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Focus" Style="{StaticResource PillButton}" Padding="10,4" Click="Focus_Click" Tag="{Binding}" />
                                                <Button Content="Close" Style="{StaticResource PillButton}" Padding="10,4" Margin="6,0,0,0" Click="Close_Click" Tag="{Binding}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>

            <TextBlock x:Name="StatusText" Foreground="{StaticResource TextDim}" Margin="0,12,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
