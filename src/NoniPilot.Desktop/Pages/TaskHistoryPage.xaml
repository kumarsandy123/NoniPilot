<UserControl x:Class="NoniPilot.Desktop.Pages.TaskHistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:NoniPilot.Desktop.Controls">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <Button DockPanel.Dock="Right" Content="Refresh" Style="{StaticResource PillButton}" Click="Refresh_Click" />
            <TextBlock Text="Task History" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" />
        </DockPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TaskIdFilterBox" Background="#0E1420" Foreground="{StaticResource TextPrimary}"
                         BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="8,6"
                         ToolTip="Optional task id to filter to a single command's steps" />
                <Button Grid.Column="1" Content="Filter" Style="{StaticResource PillButton}" Margin="8,0,0,0" Click="Filter_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <ListView x:Name="EventsList" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimary}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Time" Width="150" DisplayMemberBinding="{Binding TimeLabel}" />
                        <GridViewColumn Header="Actor" Width="90" DisplayMemberBinding="{Binding Actor}" />
                        <GridViewColumn Header="Action" Width="220" DisplayMemberBinding="{Binding Action}" />
                        <GridViewColumn Header="Target" Width="300" DisplayMemberBinding="{Binding Target}" />
                        <GridViewColumn Header="Outcome" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <controls:StatusPill State="{Binding OutcomeState}" Text="{Binding Outcome}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
